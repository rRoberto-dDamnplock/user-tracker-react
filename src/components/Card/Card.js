 import styles from './Card.module.scss'
import UserForm from '../UserForm/UserForm';

 const Card = () => {
      return (
            <div className={styles.card}>


            </div>
      )
 }

 export default Card;